# КУРСОВАЯ РАБОТА

**Тема:** Оптимизация рукопожатия TLS через сжатие сертификатов (RFC 8879) на основе Ideco NGFW: время TLS-рукопожатия и проверка поддержки RFC 8879

---

**Плейсхолдеры оформления (заполнить по методичке вуза):**  
ВУЗ: ______________________ Кафедра: ______________________  
Направление / специальность: ______________________  
Студент: ______________________ Группа: ______________________  
Руководитель: ______________________ Подпись, дата: ______________________  
Город: ______________________ 20___ г.

---

## СОДЕРЖАНИЕ

| Раздел | Стр. |
|--------|------|
| Введение | ___ |
| 1. Теоретические основы TLS 1.3 и сжатия сертификатов (RFC 8879) | ___ |
|  1.1. Роль рукопожатия TLS и объём цепочек сертификатов | ___ |
|  1.2. TLS 1.3 и контекст для посредников (middlebox) | ___ |
|  1.3. Механизм RFC 8879: согласование и формат сжатых сертификатов | ___ |
|  1.4. Объём данных на линии, CPU и условия заметного выигрыша | ___ |
| 2. Ideco NGFW как объект исследования и сценарии TLS | ___ |
|  2.1. Назначение NGFW и использование TLS в периметре | ___ |
|  2.2. Программно-аппаратная основа и документация v21 | ___ |
|  2.3. Связь темы работы с задачами периметра | ___ |
| 3. Методика и практический эксперимент на стенде | ___ |
|  3.1. Аппаратно-программный стенд | ___ |
|  3.2. Подготовка среды VirtualBox | ___ |
|  3.3. Проверка поддержки RFC 8879 (support check) | ___ |
|  3.4. Измерение времени установления TLS | ___ |
| Заключение | ___ |
| Список литературы | ___ |

*Примечание: номера страниц проставить после вёрстки в текстовом редакторе.*

---

## ВВЕДЕНИЕ

**Актуальность.** Большая часть прикладного трафика в сетях передаётся с использованием TLS. При установлении соединения по TLS 1.3 значимая доля байт приходится на сообщения с цепочкой сертификатов X.509. Увеличение размера цепочки удлиняет передачу на канале (особенно при ограничениях TCP в начале сессии) и нагружает процессор при криптографических операциях. Стандарт RFC 8879 «TLS Certificate Compression» задаёт согласуемое сжатие цепочки сертификатов при TLS 1.3 и новее, что позволяет уменьшить объём передаваемых данных на этапе рукопожатия без изменения семантики проверки сертификатов после распаковки.

**Объект исследования** — процесс установления TLS-сессии (рукопожатие TLS 1.3) в корпоративной среде с участием межсетевого экрана нового поколения.

**Предмет исследования** — механизм сжатия сертификатов по RFC 8879 (расширение `compress_certificate`, сообщение `CompressedCertificate`) и экспериментальная оценка признаков его согласования и метрик времени установления TLS на учебном стенде с Ideco NGFW версии 21.

**Цель работы** — систематизировать теоретические сведения о RFC 8879 в связке с TLS 1.3 и выполнить на стенде с Ideco NGFW v21 практическую проверку поддержки сжатия сертификатов и измерение времени установления TLS в выбранных сценариях.

**Задачи:**

1. Изучить положения RFC 8879 и необходимый контекст TLS 1.3 (RFC 8446) в части сообщений рукопожатия с сертификатами.  
2. Описать роль NGFW и типовые сценарии TLS для Ideco NGFW по официальной документации v21.  
3. Развернуть виртуальный стенд (VirtualBox, Ideco NGFW 21, лицензия Enterprise demo) и зафиксировать его конфигурацию.  
4. Выполнить проверку поддержки RFC 8879 (клиентские расширения, анализ трафика при необходимости).  
5. Собрать серию измерений времени установления TLS и оформить результаты в табличном виде.  
6. Сформулировать выводы по результатам эксперимента и ограничениям методики.

**Методология** — анализ нормативных документов IETF; обзор официальной документации продукта; натурный эксперимент на виртуальном стенде; регистрация сетевых трасс и/или выводов диагностических утилит; обработка серии измерений (среднее арифметическое, медиана, при возможности 95-й перцентиль).

---

## 1. ТЕОРЕТИЧЕСКИЕ ОСНОВЫ TLS 1.3 И СЖАТИЯ СЕРТИФИКАТОВ (RFC 8879)

### 1.1. Роль рукопожатия TLS и объём цепочек сертификатов

TLS обеспечивает конфиденциальность, целостность и аутентификацию сторон. На этапе рукопожатия стороны согласуют версию протокола и параметры, обмениваются ключевым материалом и, как правило, предъявляют сертификаты для проверки цепочки доверия до доверенного корня.

В типичном сценарии сервер передаёт не один конечный (leaf) сертификат, а цепочку: собственный сертификат и один или несколько промежуточных сертификатов УЦ. Поля сертификатов в DER-кодировке содержат повторяющиеся шаблоны (в том числе ASN.1-структуры и идентификаторы объектов), из-за чего цепочка поддаётся сжатию без потери информации на уровне согласованного алгоритма.

Объём цепочки влияет на время передачи по сети: при фиксированной пропускной способности и задержке (RTT) увеличение числа байт до завершения рукопожатия увеличивает наблюдаемое время до готовности канала к прикладным данным.

### 1.2. TLS 1.3 и контекст для посредников (middlebox)

В TLS 1.3 по сравнению с TLS 1.2 изменена структура рукопожатия: часть сообщений после `ServerHello` передаётся в защищённом виде, что снижает возможность пассивного анализа содержимого промежуточными узлами, не участвующими в сеансе как доверенные стороны.

RFC 8879 прямо указывает, что расширение сжатия сертификатов поддерживается **только** с TLS 1.3 и новее; при согласовании TLS 1.2 или более ранней версии узлы **должны игнорировать** это расширение. Отдельно в RFC 8879 отмечена совместимость с посредниками: часть middlebox пытается проверять сообщение `Certificate`; узлы, не понимающие сообщение `CompressedCertificate`, могут вести себя некорректно. Поэтому механизм позиционируется для версий TLS, где соответствующие сообщения недоступны для такой проверки (TLS 1.3 и выше).

### 1.3. Механизм RFC 8879: согласование и формат сжатых сертификатов

**Расширение.** Определено расширение типа `compress_certificate` (значение 27 в реестре расширений TLS). Оно может передаваться клиентом в `ClientHello` (поддержка сжатия серверной цепочки) и сервером в расширении `CertificateRequest` при взаимной аутентификации (поддержка сжатия клиентских сертификатов). Поле `extension_data` содержит список алгоритмов сжатия, которые отправитель **готов распаковывать** у peer.

Стандартом перечислены алгоритмы: zlib (1), brotli (2), zstd (3) — с отсылкой к RFC 1950, RFC 7932 и RFC 8478 соответственно.

**Сообщение.** Если peer объявил поддержку, сторона **может** сжать своё сообщение `Certificate` и отправить вместо него сообщение `CompressedCertificate` (тип рукопожатия 25). Структура сообщения включает:

- выбранный алгоритм сжатия (должен входить в список, объявленный peer в `compress_certificate`);  
- `uncompressed_length` — длина сообщения `Certificate` после распаковки;  
- `compressed_certificate_message` — сжатые байты.

Поле `uncompressed_length` позволяет получателю заранее ограничить буфер распаковки и не выполнять неограниченно большую декомпрессию. Если после распаковки длина не совпадает с объявленной, получатель **должен** прервать соединение с алертом `bad_certificate`. При невозможности распаковки соединение также **должно** быть завершено с `bad_certificate`.

После успешной распаковки сообщение обрабатывается так же, как обычное несжатое сообщение `Certificate` (парсинг и проверка доверия не меняют модель безопасности по сравнению с передачей без сжатия).

В разделе соображений безопасности RFC 8879 подчёркивает, что цепочки сертификатов обычно не дают произвольному прикладному уровню контролировать отдельные фрагменты так, как это было опасно при сжатии прикладных данных в TLS (исторические атаки на сжатие вроде CRIME касались иного контекста). Реализациям рекомендуется ограничивать память при распаковке; размер результата не должен превышать объявленный и общие лимиты TLS на размер сообщения.

### 1.4. Объём данных на линии, CPU и условия заметного выигрыша

Сжатие уменьшает число байт на проводе, но добавляет работу CPU на сжатие (на стороне отправителя) и распаковку (на стороне получателя). На высокоскоростных каналах с низкой задержкой выигрыш по времени может быть мал; на каналах с заметным RTT и при «узком» начальном окне TCP уменьшение объёма полёта данных рукопожатия может сократить время до завершения обмена, если иначе передача сертификатов растягивалась бы на дополнительные круги ожидания.

Сравнение алгоритмов zlib, brotli и zstd с точки зрения компромисса «степень сжатия / скорость» относится к характеристикам самих алгоритмов и конкретной реализации; в рамках курсовой достаточно зафиксировать, что RFC 8879 допускает любой из трёх стандартизованных методов, выбранный из пересечения поддерживаемых стороной наборов.

Итог теоретической части: RFC 8879 — опциональный механизм TLS 1.3+, уменьшающий объём передачи цепочки сертификатов при согласии сторон и соблюдении правил формата и лимитов распаковки.

---

## 2. IDECO NGFW КАК ОБЪЕКТ ИССЛЕДОВАНИЯ И СЦЕНАРИИ TLS

### 2.1. Назначение NGFW и использование TLS в периметре

Межсетевой экран нового поколения (NGFW) объединяет фильтрацию по портам и протоколам с контролем на прикладном уровне, политиками безопасности, журналированием и другими функциями периметра.

Продукт **Ideco NGFW** в корпоративной постановке используется как единая точка управления доступом в Интернет, VPN, публикацией внутренних сервисов и комплексом политик безопасности. Сведения о конкретных модулях (веб-интерфейс администрирования по HTTPS, VPN-клиент, обратный прокси, инспекция HTTPS в рамках политик контента и т.д.) следует приводить **по официальной документации** Ideco NGFW v21, с указанием разделов и страниц при цитировании в окончательной версии работы в формате, требуемом вузом.

В доступных вспомогательных материалах к данной работе отмечено, что **прямого** технического описания стандарта RFC 8879 в документации Ideco не выделялось; это не означает отсутствие поддержки на уровне сторонних библиотек или конкретных сценариев, но перенос свойств «по умолчанию» в текст курсовой без проверки на стенде недопустим.

### 2.2. Программно-аппаратная основа и документация v21

Для версии 21 полным сборником документации служит файл [Ideco NGFW v21 PDF](https://docs.ideco.ru/v21.pdf). В главе следует кратко описать назначение продукта и перечень функций, **непосредственно опираясь на оглавление и формулировки из этого PDF** (при сдаче работы вставьте 2–3 цитаты с номерами страниц).

Сведения о базовой операционной системе (в т.ч. использовании дистрибутива семейства Fedora) приводятся **только из официальных источников Ideco или v21.pdf**; если в PDF явно не указана версия ядра, в курсовой её не указывать.

### 2.3. Связь темы работы с задачами периметра

Даже без утверждения о встроенной реализации RFC 8879 в конкретном релизе, тема работы остаётся прикладной: периметр агрегирует множество параллельных TLS-сессий. Любая оптимизация объёма и длительности рукопожатия на пути «клиент — сервис» (включая прохождение через NGFW, терминацию или инспекцию TLS в разрешённых политиками случаях) влияет на задержки и загрузку вычислительных ресурсов. Практическая часть данной работы направлена на **проверяемую** оценку: объявляется ли сжатие сертификатов в тестируемых цепочках вызовов и каковы измеряемые интервалы установления TLS на учебном стенде.

---

## 3. МЕТОДИКА И ПРАКТИЧЕСКИЙ ЭКСПЕРИМЕНТ НА СТЕНДЕ

### 3.1. Аппаратно-программный стенд

| Компонент | Значение (заполнить при необходимости) |
|-----------|----------------------------------------|
| Хост | ПК, процессор AMD Ryzen 5 7500F, ОЗУ 32 ГБ DDR5-6000 МГц |
| Гипервизор | Oracle VirtualBox (указать версию: ________) |
| ВМ Ideco NGFW | Версия **21**, лицензия **Enterprise demo** |
| ВМ клиента (рекомендуется) | Linux (дистрибутив: ________, версия: ________) для единообразных замеров `openssl`/`curl` |
| Схема сети | Краткое описание: LAN/WAN, NAT, адреса (заполнить): _________________________ |

### 3.2. Подготовка среды VirtualBox

Рекомендуется (как гипотезы снижения «шума» эксперимента; отметьте, что сделано):

1. Сетевой адаптер ВМ — **VirtIO** (или рекомендуемый типом в документации Ideco для VirtualBox), избегать эмуляции устаревших адаптеров без необходимости.  
2. Выделить ВМ **фиксированный** объём RAM и **не** использовать динамическое расширение памяти гостя, если это ухудшает стабильность задержек.  
3. Зафиксировать число vCPU: ________ (не менять в ходе серии замеров).  
4. Убедиться в синхронизации времени (NTP) на Ideco согласно регламенту из документации — для корректной валидации сертификатов при тестах HTTPS.

**Статус проверки гипотез на стенде:** выполнено / частично / не выполняно: ________

### 3.3. Проверка поддержки RFC 8879 (support check)

Цель шага — установить факт: **предлагает ли клиент** расширение `compress_certificate` и **отвечает ли сервер** сообщением `CompressedCertificate` (тип 25) или обычным `Certificate` (тип 11), для выбранных целей.

**Шаг A. Версии инструментов на клиенте**

Выполнить и вставить вывод в приложение к работе или в таблицу:

```text
openssl version -a
curl --version
```

| Параметр | Значение |
|----------|----------|
| Версия OpenSSL на клиенте | ________ |
| Поддержка TLS 1.3 в клиенте | да / нет (по выводу) ________ |

**Шаг B. Подключение с принудительным объявлением алгоритмов сжатия (если поддерживается вашей сборкой OpenSSL)**

Синтаксис ключей для сжатия сертификатов зависит от версии OpenSSL. Откройте справку: `man s_client` или `openssl s_client -help` и найдите опции, связанные с **certificate compression** (например, в ветках 3.2+ встречается `-cert_comp` для перечисления алгоритмов). Запишите **фактически использованную** команду:

```text
(вставить сюда рабочую команду openssl s_client)
```

Повторить для целей (отдельная строка в журнале на каждую цель):

| № | Цель (хост:порт) | Описание сценария | Расширение compress_certificate в ClientHello (да/нет) | Ответ: Certificate / CompressedCertificate / не удалось | Примечание |
|---|------------------|-------------------|--------------------------------------------------------|------------------------------------------------------------|-------------|
| 1 | __________:443 | Веб-интерфейс Ideco | ___ | ___ | ___ |
| 2 | __________:443 | Ресурс за reverse proxy на Ideco | ___ | ___ | ___ |
| 3 | __________:443 | Внешний HTTPS-узел (например, публичный сервер) через Ideco | ___ | ___ | ___ |

**Шаг C. Анализ трафика (по желанию, но желательно)**

На Ideco или на клиенте выполнить захват (пример фильтра):

```bash
tcpdump -i any -w tls-handshake.pcap 'tcp port 443'
```

Открыть `tls-handshake.pcap` в Wireshark (указать версию: ________). В пакете `ClientHello` проверить наличие расширения с типом **27**. В ответе сервера в записи TLS 1.3 проверить тип сообщения рукопожатия **11** или **25**.

| Файл трасс | Размер, МБ | Наблюдение по RFC 8879 (кратко) |
|------------|------------|----------------------------------|
| tls-handshake.pcap | ___ | ___ |

**Итог support check (одно предложение для заключения):**  
«На стенде для целей … наблюдается …» — заполнить: _________________________________________________

### 3.4. Измерение времени установления TLS

**Метрика (рекомендуется).** Разница `time_appconnect - time_connect` в выводе `curl`, что соответствует интервалу от завершения TCP до завершения TLS-рукопожатия (без учёта времени получения HTTP-тела).

Создать файл формата, например `curl-format.txt`:

```text
time_namelookup:  %{time_namelookup}\n
time_connect:     %{time_connect}\n
time_appconnect:  %{time_appconnect}\n
time_pretransfer: %{time_pretransfer}\n
time_starttransfer: %{time_starttransfer}\n
time_total:       %{time_total}\n
```

Пример запуска серии (подставить URL):

```bash
for i in $(seq 1 30); do
  curl -sS -o /dev/null -w "@curl-format.txt" "https://ВАШ_ХОСТ/" 
done > curl-timings.txt
```

**Параметры серии:**

| Параметр | Значение |
|----------|----------|
| Число повторов N | ________ (рекомендуется не менее 20–30) |
| Условия | «Холодный» DNS / прогретый кэш (указать): ________ |
| URL сценария 1 | ________ |
| URL сценария 2 | ________ |

**Обработка результатов.** Из значений `time_appconnect - time_connect` вычислить среднее, медиану и при возможности 95-й перцентиль (можно в Excel, Python или `datamash`).

| Сценарий | N | Среднее, с | Медиана, с | p95, с | Примечание |
|----------|---|------------|------------|--------|------------|
| Сценарий 1 | ___ | ___ | ___ | ___ | ___ |
| Сценарий 2 | ___ | ___ | ___ | ___ | ___ |

**Ограничение методики (обязательно для текста).** Сравнение «с RFC 8879 / без RFC 8879» на одном и том же узле возможно только если есть контроль над сервером или известен тестовый сервер с переключаемой поддержкой сжатия сертификатов. Если на всех проверенных целях сервер не отправляет `CompressedCertificate`, таблицы отражают **фактические** задержки для реальных цепочек и не доказывают отрицательный эффект RFC 8879 в принципе — только отсутствие его использования в данных сессиях.

---

## ЗАКЛЮЧЕНИЕ

В работе рассмотрены цели и механизм RFC 8879 в контексте TLS 1.3: расширение `compress_certificate`, допустимые алгоритмы, сообщение `CompressedCertificate` и требования к обработке ошибок распаковки. Описан объектный контур Ideco NGFW v21 с отсылкой к официальной документации и оговоркой о необходимости подтверждать продуктовые детали по [v21.pdf](https://docs.ideco.ru/v21.pdf).

На учебном стенде (VirtualBox, Ideco NGFW 21, лицензия Enterprise demo, хост Ryzen 5 7500F / 32 ГБ ОЗУ) выполнены:

- проверка поддержки RFC 8879 по результатам: _________________________________;  
- измерения времени установления TLS: основные цифры сведены в табл. разд. 3.4.

Практические выводы: _________________________________________________________  
Направления доработки стенда (контролируемый TLS-сервер, единый клиент, учёт RTT): _______________

---

## СПИСОК ЛИТЕРАТУРЫ

1. RFC 8879: TLS Certificate Compression. URL: https://www.rfc-editor.org/rfc/rfc8879.html  
2. Ideco NGFW. Документация v21 (PDF). URL: https://docs.ideco.ru/v21.pdf  
3. RFC 8446: The Transport Layer Security (TLS) Protocol Version 1.3. URL: https://www.rfc-editor.org/rfc/rfc8446.html  
4. Документация OpenSSL к версии, использованной на стенде (справка `man s_client`, `man openssl` или сайт проекта OpenSSL).  
5. Wireshark User’s Guide / Display Filter Reference: TLS (для интерпретации расширения 27 и типов сообщений 11/25). URL: https://www.wireshark.org/docs/  

*При требовании кафедры оформить библиографию по ГОСТ Р 7.0.5–2008 — переформатировать пункты 1–5 в соответствии с методичкой.*

---

*Конец текста курсовой работы (черновик для переноса в шаблон вуза).*
